2026-05-19 TuShare data planning: verified repo path `/Data/lzp/MacroQuant`, checked memory/GPU before/after API probes, and ran small HTTP permission probes without saving data. Core daily, financial VIP, historical minute, auction, industry, margin/moneyflow, shareholder/event, report, major news, CCTV news, and research report interfaces are accessible. `anns_d` has no permission; `fina_audit` requires per-stock params. Added `docs/tushare_data_download_plan.md`. After user approval, stored `TUSHARE_TOKEN` in ignored local `.env`.
2026-05-19 P0 TuShare download: fixed `.gitignore` root anchoring so `/data/` ignores do not hide `scripts/data/`, added `scripts/data/download_tushare_p0.py`, and downloaded P0 raw tables to ignored `data/raw/tushare/p0/`. Command ended successfully; output is ~1022M across 3043 Parquet files. Counts: stock_basic 5844, stock_company 6271, trade_cal 11966, bak_basic 10269338, namechange 8244, index_classify 511, index_member_all 5847. `bak_basic` first nonempty date is 20160809; BSE `trade_cal` returned 0 rows. User clarified news/research interfaces were not purchased, so they are not default data sources.
2026-05-19 P0 quality audit: wrote ignored report `results/data_quality/p0_audit_20260519_153316.json`. Files/meta counts match: 3043 Parquet and 3043 sidecar JSON. `stock_basic`, `index_classify`, `index_member_all`, and SSE/SZSE `trade_cal` pass key/partition checks. Issues: `namechange` monthly download is incomplete and must be redone by `ts_code`; `bak_basic` has 173 zero-row trade-date partitions, including 26 after first nonempty date, with sampled dates still returning 0 from `bak_basic` while `daily` has rows; BSE `trade_cal` is empty; `stock_company` misses 16 `stock_basic` codes and has 12 blank `com_name` rows.
2026-05-19 Namechange supplement: added `scripts/data/supplement_tushare_namechange.py` and ran per-`ts_code` supplement for all 5844 `stock_basic` codes. Raw per-code rows: 33740; per-code full-row dedup: 19866 rows; 5843 codes have rows and `TS0018.SH` is the only zero-row code. Created current best union table `data/raw/tushare/p0/namechange_combined/namechange_union_dedup.parquet` by unioning per-code dedup with legacy monthly data; final union has 19905 rows, no duplicate full rows, no blank `ts_code`. Use `ann_date`/available_at for PIT because max `start_date` is 20260520.
2026-05-19 P0 TuShare repeatable audit: added `scripts/data/audit_tushare_p0.py` and ran `~/miniconda3/bin/python scripts/data/audit_tushare_p0.py --p0-root data/raw/tushare/p0 --output-dir results/data_quality`. Final report: `results/data_quality/p0_audit_20260519_181546.json` and `.md`; exit 0 with 0 errors, 5 warnings, 13 infos. Inventory now has 8889 Parquet files and 8889 sidecars, no temp files. Main warnings: 12 blank `stock_company.com_name`, 16 `stock_basic` codes missing from `stock_company` plus 443 company-only codes, known `bak_basic` zero-row source dates (173 total, 26 after first nonempty), legacy monthly `namechange` incomplete but supplemented by per-code/union table, and 9 `index_member_all` codes absent from `stock_basic`.
2026-05-19 P0 storage/script cleanup: flattened `data/raw/tushare/p0/*` into `data/raw/*`, removed duplicate `namechange` raw variants, and kept only `data/raw/namechange/namechange.parquet` as the final union-dedup table. Cleaned `results/data_quality/` to a single `p0_status.json`. Replaced three data scripts with one `scripts/data/tushare_data.py` supporting `download-p0` and `audit-p0`. New audit command `~/miniconda3/bin/python scripts/data/tushare_data.py audit-p0 --raw-dir data/raw --output results/data_quality/p0_status.json` exits 0 with 0 errors and 5 known warnings; inventory is now 2607 Parquet files and 2607 sidecars, data size about 1018M.
2026-05-19 TuShare `bak_daily` vs `bak_basic` check: used the TuShare MCP tools plus local token probes to compare selected dates. `bak_daily` is not globally more complete: it is empty before its 2017-mid coverage window where `bak_basic` can have rows, but it can fill some later `bak_basic` holes such as 20200102. Recommendation is to keep `bak_basic` and add `bak_daily` as a supplemental backup daily table if we want better gap coverage, not replace `bak_basic`.
2026-05-19 P1 TuShare download: extended the single `scripts/data/tushare_data.py` utility with `download-p1` and `audit-p1`, then downloaded daily, adj_factor, daily_basic, stk_limit, suspend_d, and optional limit_list_d into flat `data/raw/*` directories. `stk_limit` hit TuShare 400 calls/minute once; the script now retries rate-limit responses and P1 defaults to a safer 0.18s interval. Final P1 audit `results/data_quality/p1_status.json` is ok with 0 errors and 0 warnings. Counts: daily 13,919,434 rows, adj_factor 14,575,372, daily_basic 13,828,328, stk_limit 16,357,937, suspend_d 467,341, limit_list_d 153,459. Current raw parquet/meta inventory is 24,014 each; data/raw is about 3.9G.
2026-05-19 P0/P1 semantic audit: added `audit-semantics` to `scripts/data/tushare_data.py` and wrote `results/data_quality/data_semantics_status.json` (status warning, 0 errors, 6 warnings). P1 partitions/sidecars are complete. Key findings: `bak_basic` has no volume/amount fields and must not be used for turnover alignment; `bak_daily.amount` is inferred as 万元 and must be multiplied by 10 to compare with `daily.amount` 千元; `bak_daily` is useful as a supplemental backup quote source but not a replacement. Latest local `bak_basic` 20260519 is stale/zero while live API now returns 5523 rows; refresh only if that current-day snapshot is needed. Daily/daily_basic coverage differs mostly for BJ codes; adj_factor extra rows are reasonable; `stk_limit` includes A/B/funds and cannot be judged by row count vs daily. Raw files still need PIT available_at handling; same-day `daily`/`daily_basic` must not feed 09:25 decisions.
2026-05-20 P2 TuShare download: extended `scripts/data/tushare_data.py` with `download-p2` and `audit-p2`, downloaded P2 financial/fundamental data to flat `data/raw/*`, and wrote `results/data_quality/p2_status.json`. Final P2 audit status is warning with 0 errors and 5 source/semantic warnings. Counts: income_vip 342,098 rows, balancesheet_vip 346,082, cashflow_vip 301,154, fina_indicator_vip 523,405, forecast_vip 131,542, express_vip 27,912, dividend 167,859, fina_audit 95,973, fina_mainbz_vip 2,090,826, disclosure_date 253,063. P2 has 18,251 Parquet files and matching sidecars; all raw now has 42,265 Parquet/sidecars and is about 4.7G. Warnings are duplicate/multi-version financial keys, sparse event partitions, and some missing dividend/fina_indicator ann_date values; no broad redownload required.
2026-05-24 P0/P1/P2 integrated data quality audit: added `audit-integrated` to `scripts/data/tushare_data.py`, refreshed P0/P1/P2/semantic status files, and wrote `results/data_quality/p0_p1_p2_integrated_status.json`. Final integrated status is warning with 0 errors, 15 warnings, and 36 info items; P0/P1/P2 inventory is 42265 Parquet files and 42265 sidecars with no missing/orphan sidecars or tmp files. Main findings are known source/semantic issues: `bak_basic` 20260519 local partition is stale/zero while live API returns rows, `daily`/`daily_basic` and `stk_limit` coverage differ by expected BJ/fund/board semantics, `bak_daily.amount` is万元 vs `daily.amount`千元, and P2 financial/dividend tables require PIT version selection and event-date handling. No broad P1/P2 redownload is needed; targeted `bak_basic` 20260519 refresh and optional `bak_daily` download remain optional.
2026-05-24 TuShare script/status consolidation: kept `scripts/data/tushare_data.py` as the single data utility but reduced the public CLI to `download --tier {p0,p1,p2}` and `audit`; removed obsolete per-tier/semantic/integrated audit wrapper commands from argparse and slimmed the script from 2087 to 1748 lines. Unified status is now `results/data_quality/status.json`, generated by `~/miniconda3/bin/python scripts/data/tushare_data.py audit --include-limit-list --probe-api --end-date 20260519 --p2-end-date 20260520 --output results/data_quality/status.json`. Final status: warning, 0 errors, 16 warnings, 40 infos. Existing old status files are historical/local only and should not be hand-maintained.
2026-05-24 Heuristic Learning framework scaffold: reviewed updated `docs/heuristic_learning_trading_system.md`, which elevates TradeStrategyPolicy, daily replay, broker constraints, event checkpoints, and trial ledger to first-class objects. Added a lightweight `src/hl_trader/` framework with schemas, TuShare PIT data contracts/readers, WFO splitter, daily BrokerSimulator/replay skeleton, formulaic scoring, portfolio weights, evaluation metrics, and JSONL TrialLedger. Added `configs/experiments/pilot_2020_daily.yaml` for a 2020+ daily value/quality pilot and `docs/quant_framework_notes.md`. Verified with `PYTHONPATH=src ~/miniconda3/bin/python -m compileall -q src tests` and `PYTHONPATH=src ~/miniconda3/bin/python -m unittest discover -s tests -p 'test_*.py' -v` (9 tests OK).
2026-05-24 HL pre-LLM API framework hardening: completed the steps needed before wiring a large-model API. Added PIT daily feature construction and leakage checks, formulaic WFO runner, daily replay/broker hardening, and offline evidence/event/NL shadow modules. SubAgent audits were run and closed for PIT, WFO/replay, and evidence/event/NL shadow; their fixes tightened no-future data checks, unit metadata, WFO window boundaries, T+1/limit/turnover constraints, stable hashes, and NL shadow isolation. Final verification: `PYTHONPATH=src ~/miniconda3/bin/python -m compileall -q src tests` passed and `PYTHONPATH=src ~/miniconda3/bin/python -m unittest discover -s tests -p 'test_*.py' -v` passed with 36 tests OK; GPU/RAM checks stayed stable.
2026-05-25 LLM shadow API integration with DeepSeek provider: checked official DeepSeek OpenAI-compatible docs, then added `src/hl_trader/llm/` DeepSeek JSON-mode client plus provider-agnostic `LLMShadowAdvisor`, prompt schema, NL provider metadata sanitization, and `scripts/hl/llm_shadow.py` / `src/hl_trader/pipelines/llm_shadow.py` for feature-file or evidence-JSONL shadow runs. Three SubAgent audits were run and closed for the API client, shadow advisor, and pipeline/CLI. No real DeepSeek API call was made and no balance was spent; tests use mocks/dry-run. Verification: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src ~/miniconda3/bin/python -m compileall -q src tests scripts` passed and full unit discovery passed with 65 tests OK. Secret-pattern scan outside ignored runtime dirs returned 0 file matches.
2026-05-25 Provider-agnostic LLM shadow rename audit: confirmed physical repo path `/Data/lzp/MacroQuant`, found no legacy DeepSeek-specific shadow-entrypoint naming in code/docs outside ignored runtime dirs, and kept public API/files/events/default outputs on `llm_shadow`. Minor fixes moved the generic CLI/run config model default to provider-adapter default, changed generic LLM shadow tests to `test-provider`, and updated docs wording to describe LLM shadow plus DeepSeek provider. Verification passed: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src ~/miniconda3/bin/python -m compileall -q src tests scripts`, full unit discovery with 65 tests OK, and `PYTHONPATH=src ~/miniconda3/bin/python scripts/hl/llm_shadow.py --help`. No real API call was made; GPU/RAM checks stayed safe with about 432Gi available at finish.
2026-05-25 Trusted experiment loop audit: confirmed physical repo path `/Data/lzp/MacroQuant`, hardened development WFO freeze/context handling, and kept the entrypoint explicitly development-only. `freeze_hash` now includes full track/template/protocol/policy component hashes plus horizon/model/prompt/data-contract, every ExperimentLedger event receives those context fields while retaining TrialLedger `record_hash`, and `result_available_time` now handles YYYYMMDD values while rejecting future/unparseable non-null values. Added unit coverage for freeze component drift, ledger context, tamper detection, and CLI import. Verification passed: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src ~/miniconda3/bin/python -m compileall -q src tests scripts`, targeted 14 tests OK, full unit discovery with 74 tests OK, and `PYTHONPATH=src ~/miniconda3/bin/python scripts/hl/run_experiment.py --help`. No real API call was made; GPU/RAM checks stayed safe with about 432Gi available at finish.
2026-05-25 Trusted experiment real-data smoke: added `scripts/hl/build_daily_features.py` and built real PIT daily features from local TuShare raw data for 20200102-20230703 into ignored `data/features/daily_alpha` (847 partitions, 3,838,379 rows). Ran `scripts/hl/run_experiment.py --config configs/experiments/pilot_2020_daily.yaml --features data/features/daily_alpha --max-folds 1`; it wrote ignored ledger `experiments/trial_ledger/pilot_2020_daily.jsonl`, completed 1 development fold before 2025 held-out, produced 143 fills, and returned test_return -2.6246%. Ran real-feature LLM shadow dry-run successfully with 1 evidence pack and 0 API decisions. DeepSeek live smoke was attempted but failed fast because local `DEEPSEEK_API_KEY` is missing; no API balance was consumed.
2026-05-25 Trusted experiment hardening follow-up: stored `DEEPSEEK_API_KEY` in ignored local `.env`, then tightened the development WFO loop after audit findings. `FormulaicWfoRunner` now fails fast unless `TradeStrategyPolicy.allowed_actions` includes `rebalance`; train slices require non-null `result_available_time`; `DailyPITFeatureBuilder` emits `result_available_time`; and test-window event checkpoints are written to the ledger as `log_only` / `shadow_only` / `can_affect_trading=false`. Verification passed: `compileall`, full unit discovery with 77 tests OK, `git diff --check`, and secret-pattern scan outside ignored runtime dirs returned 0. DeepSeek live JSON-mode smoke succeeded with model `deepseek-v4-flash`, returned `{"status":"ok","check":"deepseek_smoke"}`, and used 62 total tokens.
2026-05-25 Held-out control runner and v4-pro smoke: added `DailyFormulaicHeldoutRunner` and `scripts/hl/run_heldout.py` for frozen-parameter held-out Control evaluation without fitting/writeback; ExperimentLedger now supports default phase `heldout`, and tests assert held-out records stay in held-out phase with frozen params. Updated framework notes with held-out command and `deepseek-v4-pro` convention. Verification passed: targeted held-out tests OK, full unit discovery with 79 tests OK, and `run_heldout.py --help` passed. DeepSeek live JSON-mode smoke with `deepseek-v4-pro` succeeded, returned `{"status":"ok","check":"deepseek_v4_pro_smoke","model":"v4-pro"}`, and used 81 total tokens.
2026-05-25 HL cleanup, event execution, and text-tier scaffolding: consolidated `scripts/hl` to a single `scripts/hl/hl.py` subcommand entrypoint and deleted the four thin wrappers; updated tests/docs to use `build-features`, `run-development`, `run-heldout`, and `llm-shadow`. `FormulaicWfoRunner` now emits real `enter/add/trim/exit` reasons, enforces those actions as policy permissions, requires execution constraint columns, and lets frozen event rules trigger `event_de_risk` or `exit` orders while LLM shadow remains non-trading. Added optional TuShare `download --tier text/p5` plus `audit --include-text` coverage for `anns_d`, `major_news`, `cctv_news`, `npr`, `research_report`, `report_rc`, and explicit-source `news`, including PIT `available_at` fallback and expected-partition audit. Two GPT-5.5 xhigh SubAgents audited text data and implementation risks; blocking findings were fixed. No full text download was run. Verification passed: compileall, targeted WFO tests, full unit discovery with 81 tests OK, CLI help checks, `git diff --check`, and resource checks stayed safe.
2026-05-26 TuShare text download from 2020: confirmed `scripts` now only has two real entrypoints (`scripts/data/tushare_data.py` and `scripts/hl/hl.py`) plus ignored runtime logs; cleaned test `__pycache__`. Adjusted text-tier defaults to conservative 0.65s/call and 800-row default pagination, then used dataset-specific pagination for download: `anns_d` with 6000 rows/page and the remaining default text datasets with 800 rows/page. Downloaded 20200101-20260525 default P5 text data to ignored `data/raw`: anns_d 77 files/9,339,855 rows, major_news 77/2,726,571, cctv_news 2337/35,142, npr 77/8,552, research_report 77/234,836, report_rc 77/1,477,359. `news` short-message data was not downloaded because TuShare requires explicit source selection. Unified audit `results/data_quality/status_text_20200101_20260525.json` ended warning with 0 errors and 20 warnings; text warnings are duplicate business-key rows in anns_d/major_news/research_report/report_rc, not missing partitions or unparseable available_at. Verification passed: script compile, targeted 26 tests OK, CLI help checks, git diff check, and resource checks; `data/raw` is now about 12G.
2026-05-26 TuShare official-rate text follow-up: checked official TuShare docs for 10000-point frequency, independent text permissions, per-interface row limits, and news source identifiers. Updated `scripts/data/tushare_data.py` so text/P5 defaults respect official rates (`0.22s` mixed text default; `news` run used `0.16s` for the 400/min news tier), clamps text page sizes to official limits, and auto-expands `news` to all 9 official sources. Replaced `news` monthly-source partitioning with source+daily partitioning after TuShare returned 50101 on high-offset monthly pagination. Downloaded `news` all sources for 20200101-20260525: 21,033 parquet/meta files, 10,258,167 rows. Unified all-text audit `results/data_quality/status_text_20200101_20260525_all_sources.json` ended warning with 0 errors, 21 warnings, and 50 infos; no missing expected text partitions or unparseable `available_at` values. Updated TuShare plan docs and resource checks stayed safe.
2026-05-26 TuShare official-limit redownload: re-downloaded `anns_d`, `major_news`, and `npr` from 20200101-20260525 with the new official per-call clamps (`2000`, `400`, `500`) and `0.22s` interval. Final all-text audit remains warning with 0 errors, 21 warnings, 50 infos. Updated row counts: `anns_d` 9,340,185, `major_news` 2,726,603, `npr` 8,552; all expected text partitions present and all `available_at` values parse.
2026-05-26 TuShare required-interface permission probe: after deciding not to reuse/copy ChouQuant minute data, ran current small-sample TuShare HTTP probes for remaining documented P3/P4 interfaces. `stk_mins`, `stk_auction`, `stk_auction_c`, `margin`, `margin_detail`, `moneyflow`, `stk_holdernumber`, `stk_holdertrade`, `repurchase`, `share_float`, `block_trade`, and already-downloaded `report_rc` all returned code 0 with expected fields; margin/margin_detail returned rows on 20260525/20260522. No data was saved except ignored probe log `logs/tushare_required_interfaces_probe_20260526.log`. Resource checks stayed safe.
2026-05-26 P3 TuShare historical minute ingestion: added `download --tier p3` for full-A `stk_mins` 1-minute stock-year partitions plus `audit-p3` for lightweight minute-only status checks. Confirmed historical auction bars come from `stk_mins` itself, so `stk_auction`/`stk_auction_c` are not downloaded for history. Window test downloaded `000001.SZ` and `300750.SZ` for 20200101-20200131 into `data/raw/stk_mins_1min` (2 partitions, 7712 rows, 2 API pages) and `results/data_quality/status_p3_window_20200101_20200131.json` is ok with 0 errors/0 warnings; sample checks found 09:30 and 15:00 bars, no duplicate keys, and vol=shares/amount=CNY metadata. Full-A 20200101-20260525 is estimated at 5734 active codes, 35855 stock-year partitions, about 18.22B minute rows and 250.5K API calls, so it should be launched as an explicit long resumable job rather than foreground work.
2026-05-26 P3 full-A minute download start: ran and closed SubAgent `Copernicus` for editable data-download-document audit; it simplified `docs/tushare_data_download_plan.md` to 185 lines, kept P0-P5 scope/rate/unit/PIT/audit coverage, and synced the P3/P5 boundary in `docs/quant_framework_notes.md`. Started full-A TuShare `stk_mins` 1min background download for 20200101-20260525 with PID 2896717 and log `logs/tushare_p3_stk_mins_fullA_20200101_20260525_20260526.log`; command used `--force` only to overwrite the earlier partial Jan-2020 window partitions into complete stock-year partitions. Initial progress after startup: 50/35855 tasks, 2,662,809 rows, 365 API pages, 65 parquet/meta files, 0 tmp files; resource checks remained safe with about 412Gi available memory.
2026-05-26 data_quality status naming cleanup: replaced date-suffixed/top-level status outputs with semantic names. `scripts/data/tushare_data.py audit` now defaults to `base_research_status.json`, `text_evidence_status.json`, `intraday_minutes_status.json`, or `combined_status.json` by included scope; `audit-p3` defaults to `intraday_minutes_status.json`. Added semantic `download --tier` aliases (`reference`, `daily`, `fundamental`, `intraday`, `text_evidence`) while keeping old p0/p1/p2/p3/p5 aliases. Moved old window/date status files under `results/data_quality/archive/20260526_status_cleanup/`; top-level now has only `base_research_status.json` and `text_evidence_status.json` until full intraday audit completes. Verification passed: compileall, CLI help checks, `git diff --check`; P3 full-A download continued running and reached 2550/35855 tasks.
2026-05-26 root archive and audit rerun: added root `/archive/` to `.gitignore`, moved historical data-quality status files from `results/data_quality/archive/` to `archive/data_quality/20260526_status_cleanup/`, and kept `results/data_quality/` for current status files only. Re-ran the base research audit with the new default output name: `results/data_quality/base_research_status.json` is warning with 0 errors, 16 warnings, and 40 infos. `text_evidence_status.json` remains the current renamed all-sources text audit (warning, 0 errors, 21 warnings, 50 infos); it was not rerun to avoid unnecessary scanning while the full intraday minute job is active. P3 full-A download remained running under PID 2896717 and reached 2800/35855 tasks, 148,429,249 rows, and 20,323 API pages; resource checks stayed safe with about 438Gi available memory.
2026-05-26 logbook rename: renamed the concise project log from `SUMMARY.md` to `LOGBOOK.md` and the detailed durable log from `docs/summaries/SUMMARY.original.md` to `docs/logbook/DETAILED_LOGBOOK.md`; removed the empty `docs/summaries/` directory. Updated current logging instructions in `AGENTS.md` and `CLAUDE.md`, plus current data-download docs, to use the new names. Historical references inside old detailed log entries were left intact as factual command/history traces. P3 full-A minute download remained running under PID 2896717.
2026-05-26 scripts flattening: moved the two active script entrypoints from `scripts/data/tushare_data.py` and `scripts/hl/hl.py` to `scripts/tushare_data.py` and `scripts/hl.py`, removed the now-empty `scripts/data/` and `scripts/hl/` directories, and updated current docs/tests/help-text references. Historical logbook command traces were left unchanged. Verification passed: compileall, `scripts/tushare_data.py download --help`, `scripts/tushare_data.py audit --help`, `scripts/hl.py --help`, targeted 15 unit tests OK, and `git diff --check`. P3 full-A minute download stayed running under PID 2896717 and reached 4050/35855 tasks.
2026-05-26 ingestion reliability audit: compared MacroQuant TuShare retry behavior with `/Data/lzp/ChouQuant/data/update_data.py` and ran a read-only raw-data reliability scan to `results/data_quality/ingestion_reliability_status.json`. Final status is warning, not error: no parquet/meta/tmp/pagination structural issues, no current P3 log failure hits, and existing base/text data-quality status files have 0 error findings. P3 had 3 zero-row stock-year files, but all have 0 same-year local `daily` rows (`000670.SZ` 2021, `002260.SZ` 2020/2021), so they are not evidence of dropped successful trading data. One stale P3 window-test partition remains (`300750.SZ` 2020 covering only 20200101-20200131) because the full sequential job has not reached that task yet; it should be overwritten when the job reaches index 15527. The future hardening target is to retry/fail zero-row `stk_mins` responses when same-year `daily` rows exist.
2026-05-26 macro/global TuShare scaffolding: added code-only support for macro and global context downloads in `scripts/tushare_data.py` without starting new downloads. New tiers cover `cn_schedule`, GDP/CPI/PPI/PMI, money supply, social financing, Shibor/LPR/Hibor/Libor/repo, US rates, global indices, FX, `eco_cal`, and the newly unlocked `monetary_policy` interface; raw rows receive conservative `available_at`/`available_at_rule` where source release times are absent. Added `audit-macro` with default output `results/data_quality/macro_context_status.json`, macro/global unit/PIT rules, and docs updates in `docs/tushare_data_download_plan.md` and `docs/quant_framework_notes.md`. Verification passed: compileall, `download --help`, `audit-macro --help`, macro helper smoke, and `git diff --check`; no TuShare data was downloaded. P3 full-A minute job stayed running under PID `2896717` and reached about `13050/35855` tasks.
2026-05-27 P3 full-A minute download completion check: PID `2896717` had exited and the log ended with `P3 download finished`. Final TuShare `stk_mins_1min` counts: 35,855/35,855 stock-year tasks, 1,820,916,656 rows, 249,867 API pages, 35,855 parquet files, 35,855 sidecars, 0 tmp files, and about 44G under `data/raw/stk_mins_1min`. Error keyword scan of the runtime log found no Traceback/error/failed/Exception entries. A full `audit-p3` is still the next validation step before treating the minute layer as complete for research.
2026-05-27 P3 audit and macro/global context download: ran full `audit-p3` for `20200101-20260525`, wrote `results/data_quality/intraday_minutes_status.json`, and got warning with 0 errors, 1 warning, 2 infos. P3 inventory is complete: 35,855 expected files, 35,855 parquet/meta files, 0 missing files, 0 missing/orphan sidecars, 0 schema misses, and 1,820,916,656 rows. The warning is 135 zero-row stock-year partitions; local daily cross-check found 128 have same-year daily rows, mostly BJ names plus `302132.SZ`, so this needs source-coverage investigation before intraday research uses those names. Downloaded macro/global context for `20200101-20260525`: macro default plus `shibor_quote`, global default, including `monetary_policy` 25 rows, `eco_cal` 6,667 rows, `index_global` 15,782 rows, and `shibor_quote` 17,954 rows. Fixed an overly broad macro audit warning condition and reran `audit-macro`; `results/data_quality/macro_context_status.json` is warning with 0 errors, 2 warnings, 42 infos. Remaining macro warnings are `cn_schedule.data_api` blanks and duplicate `eco_cal` event business keys. Verification/resource checks passed and `git diff --check` passed.
2026-05-27 required-data completeness audit: ran full combined audit for reference/daily/fundamental/text/minute raw data with P3 and text included. Output `results/data_quality/combined_status.json` is warning with 0 errors, 22 warnings, and 51 infos; integrated filesystem inventory has 101,875 parquet files and 101,875 sidecars with no missing/orphan sidecars or tmp files. Re-ran `audit-macro`; `results/data_quality/macro_context_status.json` remains warning with 0 errors, 2 warnings, and 42 infos. Current verdict: core data needed for the first research loop (P0/P1/P2/P3/text evidence/macro-global context) is structurally complete, but not warning-free. Important residual issues are PIT/unit semantics, duplicate raw text/financial business keys, 135 zero-row minute stock-year partitions, and macro `cn_schedule`/`eco_cal` raw quirks. P4 event/flow datasets (`margin`, `margin_detail`, `moneyflow`, `stk_holdernumber`, `stk_holdertrade`, `repurchase`, `share_float`, `block_trade`) are still not downloaded, so the broader event/资金 layer is not complete if considered required.
2026-05-27 P4 event/flow download and audit: added P4 download/audit support to `scripts/tushare_data.py`, then downloaded the 20200101-20260525 event/flow window. Trade-date datasets were capped to local SSE `trade_cal` coverage through 20260519; monthly/natural-day event datasets ran through 20260525. Counts: `margin` 3,874 rows, `margin_detail` 4,817,891, `moneyflow` 7,291,707, `block_trade` 207,426, `stk_holdernumber` 304,607, `stk_holdertrade` 100,443, `repurchase` 73,427, `share_float` 6,432,361. Deleted 2 legacy `share_float/month=` parquet files plus sidecars from the failed first attempt and reran `audit-p4`. `results/data_quality/event_flow_status.json` is warning with 0 errors, 8 warnings, and 10 infos; all expected P4 partitions are present. Residual warnings are exact-limit/source-cap risk (`share_float` has 966 exact-6000 partitions and no exposed limit/offset), duplicate event business keys, and blank nullable event dates.
2026-05-27 `share_float` completion strategy trial: added `download-share-float-complete` to `scripts/tushare_data.py`. Pilot window downloaded `ann_date=20190820-20190825` plus full-A `float_date=20200106 + ts_code` rescue; the original `float_date=20200106` all-market file had 6000 rows, while ts_code rescue saw 13,628 rows and one still-capped stock (`002973.SZ` at 6000 rows). Then ran full `ann_date` first-stage scan for 20100101-20260525: 5,989 announcement-day partitions, 6,999,549 rows seen, and 976 announcement days exactly at the 6000-row source cap. Conclusion: `ann_date` is the PIT primary path but still not complete by itself; full `ann_date+ts_code` rescue would be about 5.7M calls and should be targeted, not default.
2026-05-27 `share_float` rescue guard hardening: switched verification to the `stock` conda environment (`~/miniconda3/bin/conda run -n stock python`). Extended `download-share-float-complete` with explicit `--rescue-ann-date`, `--rescue-code`, `--rescue-codes-file`, and `--max-rescue-calls` controls. Guard probe with `--float-rescue-date 20200106 --max-rescue-calls 1` failed fast before API calls, estimating 5,844 calls for one full-A rescue date. Multi-code `ts_code` probes returned 0 rows, so TuShare `share_float` does not support batching multiple stock codes in one request.
2026-05-27 `share_float` candidate rescue: changed rescue default from all-A to `--rescue-universe candidate`. Candidate codes now come from the capped partition itself, cross-path `share_float` evidence, unlock-related `anns_d` announcement titles, and explicit user codes/files; `--rescue-universe all_a` is required for full-A scans. Rechecked `float_date=20200106`: partition self-candidates were 18 codes, cross-path ann-date candidates lifted the set to 24 codes, matching all nonzero stocks found by the earlier full-A rescue; the probe skipped existing files and needed 24 tasks instead of 5,844.
2026-05-27 `share_float` full candidate supplementation: ran the full 20100101-20260525 candidate rescue for all 976 capped `ann_date` partitions. The rescue made 16,505 `ann_date+ts_code` tasks, wrote 16,505 files, saw 9,487,781 rows, and had 0 no-candidate dates; 8,784 files were zero-row candidate checks and 1,369 finest partitions still hit the 6,000-row source cap. Wrote the backup union `data/raw/share_float_complete/share_float_complete.parquet` with 12,735,947 deduplicated rows from 30,675 input files. Re-ran `audit-p4`; `event_flow_status.json` remains warning with 0 errors and 8 warnings because the original `share_float/date=` path is still capped and raw event keys still need PIT/business-key handling. Resource checks stayed safe.
2026-05-27 documentation naming and maintenance policy: renamed the active design docs to `docs/project_design_draft.md`, `docs/code_framework_design.md`, and `docs/data_documentation.md`; merged the live QMT workflow and Aliyun deployment notes into `docs/qmt_deployment_documentation.md`. Added living-documentation rules to `AGENTS.md` and `CLAUDE.md`, requiring future strategy, code-framework, data-contract, and QMT workflow changes to update the corresponding docs in the same work item. Historical logbook references to old names were left as history.
2026-05-27 QMT documentation adaptation: rewrote `docs/qmt_deployment_documentation.md` from a historical trading-workflow document into the current MacroQuant QMT standby/live-readiness document. It now states that the remote Aliyun Windows + MiniQMT environment exists, but this repo has no frozen model or active `scripts/live/` order generator yet; current operations are read-only health checks and optional dry-run only. Added future live workflow,上线门槛, payload draft, remote deployment notes, and execution guardrails.
2026-05-28 data script/document cleanup audit follow-up: closed SubAgent `Galileo` after read-only audit. Public data CLI now uses semantic tier names only, replaced `audit-p3`/`audit-p4` with `audit-intraday`/`audit-event-flow`, removed old tier compatibility aliases, and started internal renaming from P0-P5 labels toward reference/daily/fundamental/intraday/event_flow. Updated `AGENTS.md` and `CLAUDE.md` to state living docs should only describe the latest accepted state; historical details belong in logbooks. Cleaned `docs/data_documentation.md` wording, removed historical status narrative, switched commands to `conda run -n stock`, and documented current non-share_float truncation candidates for targeted refresh. After SSH recovered, verification passed: `git diff --check`, script compile, and CLI help checks for `download`, `audit`, `audit-event-flow`, `audit-intraday`, and `download-share-float-complete`.
2026-05-28 data_quality/status cleanup: clarified current docs so human-facing descriptions use Chinese data names and TuShare interface ids stay in the interface column or CLI examples. Removed the event/flow table priority column from `docs/data_documentation.md`. Moved ad hoc or duplicate status files from `results/data_quality/` to `archive/data_quality/legacy/20260528_cleanup/`: `combined_status.json`, `ingestion_reliability_status.json`, `share_float_candidate_probe.json`, and `share_float_completion_status.json`. `results/data_quality/` now keeps only current domain statuses: base research, macro context, intraday minutes, event/flow, and text evidence. Future combined/share-float process defaults now write under `results/data_quality/process/`; historical process files should move to root `archive/` when superseded. Verification passed: script compile, `audit --help`, `download-share-float-complete --help`, current docs/scripts wording scan, `git diff --check`, and resource checks; historical logbook entries still retain old paths as factual history.
2026-05-28 combined/share_float audit merge: removed default persistent combined/share-float process status outputs. A unified audit with both `--include-text` and `--include-intraday` now fails fast unless `--output` is explicit. `download-share-float-complete` no longer writes a status file by default; `--output` is optional for ad hoc process reports. `audit-event-flow` now checks `data/raw/share_float_complete/share_float_complete.parquet` directly. Re-ran `audit-event-flow`; `event_flow_status.json` is warning with 0 errors, 9 warnings, and 10 infos. The merged `share_float_complete_union` check sees 12,735,947 union rows, 12,735,947 sidecar row count, 10,697,263 source-cap-risk rows, and 1,370 rescue files still at or above the 6,000-row cap.
2026-05-28 full data coverage/unit audit: opened and closed GPT-5.5 xhigh SubAgent `Bernoulli` for a read-only audit of `docs/data_documentation.md`, `results/data_quality`, and `data/raw`. Main-thread and SubAgent metadata scans agree: current retained data items are downloaded for the documented 2020-202605 window, with trading-day datasets through 20260519 and natural-day/monthly/text/share_float datasets through 20260525 or 202605. Current top-level status files are all warning with 0 errors; raw inventory is 139,293 Parquet files and 139,293 sidecars. Unit checks found no contradiction with the data documentation; sampled minute vs daily ratios match shares/元 vs 手/千元. Residual risks are semantic/source risks, not broad missing data: `share_float` source caps, 135 zero-row minute stock-years, repeated business keys in financial/text/event raw data, `cn_schedule`/`eco_cal` quirks, and several exact-limit partitions for targeted review. `bak_daily` remains not downloaded and not part of the current retained raw boundary.
2026-05-28 targeted risk redownload: added pagination to daily trade-date downloads and force-refreshed exact-limit candidates. `daily 20221118`, `adj_factor 20220808`, and `stk_limit 20201027/20220705` now have pagination sidecars; `balancesheet_vip` exact-7000 periods were redownloaded with page_limit 7000 and expanded to 7,005-11,771 rows where applicable. `moneyflow 20230704` and `stk_holdernumber 202511` were rechecked with extra page probes. Macro sparse candidates (`cn_schedule`, `eco_cal`, `shibor_quote`, `hibor`, `libor`) were force-refreshed and row counts remained unchanged, indicating source sparsity. All 135 zero-row minute stock-year partitions were retried by year and all still returned 0 rows. Updated audit logic so exact-limit warnings ignore files with a successful extra-page probe. Refreshed status: `base_research_status.json` warning 0 errors/16 warnings, `event_flow_status.json` warning 0 errors/7 warnings, `macro_context_status.json` warning 0 errors/2 warnings, and `intraday_minutes_status.json` warning 0 errors/1 warning.
2026-05-28 first full daily experiment pass: fixed `stock` Python 3.10 compatibility (`datetime.UTC` and pandas `freq="ME"`), added `--ledger-path` overrides to `scripts/hl.py`, rebuilt PIT `daily_alpha` features for 20200102-20251231 (1,455 partitions, 7,095,173 rows), ran full development WFO (7 folds, median return -0.756%, positive fold rate 28.6%, worst -5.37%, 910 fills), and ran a held-out 2025 control with modal development parameters (`top_n=80`, PE/PB/amount quantiles 0.2/0.2/0.2) returning +4.57% with 363 fills. Result is a pipeline smoke/control, not evidence of a tradable strategy; next work should add benchmark/excess metrics, speed up WFO, and bring macro/text/event features into PIT layers.
2026-05-28 intraday by-date storage implementation: added `compact-intraday-by-date`, `update-intraday-by-date`, and `audit-intraday-by-date` to the single TuShare data script. Historical minute source remains `stk_mins_1min/ts_code=<code>/year=<year>.parquet`; the new final research/update layout is `stk_mins_1min_by_date/trade_date=<YYYYMMDD>.parquet`, with no extra persistent staging files. Added unit coverage for compaction plus audit and updated data/code framework docs. Verification passed: compileall, new CLI help checks, targeted intraday-by-date test, related existing tests, full unit discovery with 82 tests OK, and resource checks stayed safe. No real TuShare download or full historical compaction was run.
2026-05-28 final intraday/share-float storage cleanup: refreshed 2026 SSE/SZSE/BSE `trade_cal` to cover 20260525, compacted all `stk_mins_1min` stock-year source files into `data/raw/stk_mins_1min_by_date` for 1546 trade dates and 1,820,916,656 rows, and wrote current top-level `results/data_quality/intraday_minutes_status.json` with status ok, 0 errors, 0 warnings. Daily-universe coverage audit through 20260519 is warning/0 error because BJ/effective-universe mismatches remain; use a filtered minute universe in feature construction. Moved archived source/intermediate directories to `archive/data_raw/20260528_final_storage_cleanup/`: old minute stock-year source plus `share_float` float-date, ann-date, rescue, and pilot folders. Active raw now keeps only final `data/raw/stk_mins_1min_by_date` and `data/raw/share_float_complete` for these domains. Adjusted event-flow audit to treat `share_float_complete` as the retained boundary, downloaded four newly expected 20260520/21/22/25 trade-date event partitions after the calendar refresh, and reran `event_flow_status.json`: warning with 0 errors and 5 semantic warnings. Verification passed: compileall, CLI help checks, targeted intraday test, full unit discovery with 82 tests OK, and resource checks.
2026-05-28 documentation split: expanded `docs/data_documentation.md` with the five current data-domain audit code paths, status-file schema, and PIT construction/evidence-pack rules. Split `docs/code_framework_design.md` into an index plus `docs/walk_forward_environment_design.md` for PIT/WFO/replay/execution/ledger and `docs/llm_agent_design.md` for evidence packs, LLM shadow, provider adapters, prompt/response validation, and Agent upgrade boundaries. Updated `AGENTS.md` and `CLAUDE.md` living-documentation rules to include the two new docs. No code or data pipeline was changed.
2026-05-28 four-document maintenance cleanup: removed `docs/code_framework_design.md` and renamed current living docs to `docs/data_documentation.md`, `docs/agent_design.md`, `docs/environment_design.md`, and `docs/QMT_documentation.md`. Updated cross-links plus `AGENTS.md` and `CLAUDE.md` so only these four documents are maintained going forward. No code or data pipeline was changed.
2026-05-28 Agent/Environment code refactor: reorganized `src/hl_trader` into `agent/` for formulaic and LLM shadow logic, `environment/` for PIT/data/replay/execution/events/protocol/storage, and `pipelines/` for cross-layer orchestration. Moved formulaic parameters/grid/candidate selection into `agent/formulaic.py`; moved the WFO runner to `pipelines/formulaic_wfo.py`; updated CLI, tests, imports, and docs. Added architecture-boundary tests verifying `environment` does not import `agent` and old top-level source packages contain no Python modules. `compileall` passed and full unit discovery passed with 84 tests OK.
2026-05-28 pipeline documentation split: added `docs/pipeline_design.md` as the fifth living doc and moved feature-build/development/held-out/LLM-shadow orchestration details out of Agent/Environment docs. Updated `docs/environment_design.md` to focus on market/PIT/replay/execution primitives, `docs/agent_design.md` to focus on decision/evidence/provider contracts, and `docs/QMT_documentation.md`, `AGENTS.md`, `CLAUDE.md` to reference the new pipeline boundary. No code or data pipeline was run.
2026-05-28 current-doc boundary cleanup: moved the superseded `docs/project_design_draft.md` into `archive/project_design_draft.md` so `docs/` only contains the five maintained living documents plus `docs/logbook/`. No code or data pipeline was run.
2026-05-28 data_quality process cleanup: moved completed temporary process outputs `intraday_minutes_by_date_status.json` and `intraday_minutes_by_date_daily_coverage_status.json` from `results/data_quality/process/` to `archive/data_quality/20260528_process_cleanup/`. `results/data_quality/` now only keeps the five current domain status files.
2026-05-28 data documentation order cleanup: reorganized `docs/data_documentation.md` so it records data download boundaries first, audit/status rules second, and PIT construction last. Removed redundant cross-domain audit wording and kept `results/data_quality/process/` as a temporary-only path.
2026-05-28 PIT documentation boundary cleanup: narrowed `docs/data_documentation.md` from PIT construction to raw PIT data contracts, and moved selector/feature/observation responsibility into `docs/environment_design.md`. No code or data pipeline was run.
2026-05-28 TuShare update CLI split: split the monolithic TuShare entrypoint into `scripts/tushare_download.py` for download/update commands, `scripts/tushare_audit.py` for audit commands, and `scripts/tushare_core.py` for shared implementation; `scripts/tushare_data.py` remains a compatibility wrapper. Added `update --mode daily|periodic|all` for recent daily refreshes and scheduled non-daily refreshes, with optional intraday and share_float_complete updates. Updated data/pipeline docs. Verification passed: compileall, split CLI help checks, compatibility wrapper help, intraday targeted test, full unit discovery with 84 tests OK, and `git diff --check`. No real TuShare download was started.
2026-05-28 TuShare script package cleanup: moved the formal TuShare entrypoints under `scripts/tushare/` and split the 5271-line core into `common.py`, `download_ops.py`, and `audit_ops.py`; root `scripts/tushare_*.py` files are now thin compatibility wrappers. `update --mode periodic/all` now refreshes recent active month/year/period partitions by default so daily runs do not miss same-month or same-year new rows; heavyweight per-ts-code fundamental long tables are explicit via `--fundamental-datasets`. Updated data/pipeline docs. Verification passed: compileall, new and compatibility CLI help checks, targeted intraday by-date test, full unit discovery with 84 tests OK, and `git diff --check`. No real TuShare download was started.
2026-05-28 TuShare final script simplification: removed root TuShare compatibility wrappers and merged inner `download_ops.py`/`audit_ops.py` into the two formal scripts. Current TuShare script surface is only `scripts/tushare/download.py`, `scripts/tushare/audit.py`, and shared `scripts/tushare/common.py`. Updated tests and living docs. Verification passed: compileall, formal CLI help checks, targeted intraday by-date test, full unit discovery with 84 tests OK, and `git diff --check`. No real TuShare download was started.
2026-05-28 TuShare update to 20260528: ran daily/periodic raw update through `scripts/tushare/download.py`. Initial runs exposed two split-helper bugs and an overly heavy periodic reference refresh; fixed shared helpers in `common.py`, made periodic reference static-table refresh require explicit `--force`, and added default skip-existing backfill for new-stock `dividend`/`fina_audit`/`fina_mainbz_vip` partitions. Updated data through `20260528` for daily, event/flow, macro/global, and text evidence windows; full-market intraday and `share_float_complete` remain explicit opt-ins and were not refreshed. Re-ran statuses: base/event/macro/text are warning with 0 errors; intraday status remains ok from the prior by-date audit. Verification passed: compileall, `PYTHONPATH=src` full unit discovery with 84 tests OK, and `git diff --check`.
2026-05-29 TuShare daily update default expansion: changed `scripts/tushare/download.py update` so full-market by-date minute refresh and `share_float_complete` refresh are enabled by default, with `--no-include-intraday` and `--no-include-share-float-complete` available for lightweight runs. `share_float_complete` now refreshes recent raw windows but rebuilds the union over the full retained historical range to avoid truncating the union to the recent lookback. Updated `docs/data_documentation.md`. Verification passed: compileall, update/share-float CLI help, full unit discovery with 84 tests OK, and `git diff --check`. No real full-market minute/share-float update was launched in this change.
2026-05-29 TuShare default update launch/debug: launched real `update --mode all --end-date 20260529` with default intraday and `share_float_complete` enabled. The first run exposed three bugs/edge cases: same-day `daily`/`daily_basic`/event-flow zero-row source responses were written as active files, existing by-date minute files were redownloaded for tiny historical universe gaps, and `share_float_complete` union rebuild lost history after process dirs had been archived. Fixed zero-row required partition writes to `skipped_write`, added existing-minute missing-code tolerance/default skip, and made share-float union scan both active raw and archived process roots. Retry completed: 20260526-20260528 minute days were written, 20260529 skipped because expected daily universe was empty, and share-float union was restored to 12,736,101 rows. Archived stale zero-row 20260529 probe files. Refreshed audits: intraday status ok with 0 errors/0 warnings through 20260528; event-flow status warning with 0 errors/5 semantic warnings. Verification passed: compileall, targeted intraday skip probe, full unit discovery with 84 tests OK, and `git diff --check`.
2026-05-29 TuShare editable audit hardening: independently reviewed the current `scripts/tushare/` download/update/audit path and tightened remaining guards without starting large downloads. Generic `event_flow` download now excludes/rejects `share_float` so 解禁 must use `download-share-float-complete`; `share_float_complete` union rebuilds fail fast if they would shrink an existing union unless `--allow-union-shrink` is explicit; intraday by-date update refuses zero-row writes when the expected universe is nonempty; audits now surface daily/fundamental exact-limit partitions and zero-row by-date minute files. Updated `docs/data_documentation.md` and added focused unit tests. Verification passed: compileall, targeted TuShare tests, full unit discovery with 89 tests OK, CLI help checks, and `git diff --check`; only temp test data was written.
2026-05-29 TuShare update simplification: replaced `update --mode daily|periodic|all` with a single daily update entrypoint, `scripts/tushare/download.py update --start-date <YYYYMMDD> --end-date <YYYYMMDD>`. The command now fills all retained data domains from the chosen start date to the end date, skips existing complete partitions by default, refreshes range partitions only when their sidecar does not cover the requested end date, and keeps `--force` for intentional full rewrites. Updated current data documentation and added a unit guard for stale range sidecars. Verification passed: compileall, update/share-float help checks, targeted TuShare tests with 7 tests OK, full unit discovery with 90 tests OK, and `git diff --check`; no live TuShare API call or large download was run.
2026-05-29 TuShare update editable audit: reviewed the single `update --start-date/--end-date` path for old mode/lookback residue, domain coverage, skip-existing behavior, sidecar coverage, and 20260529 retry risks. No old `--mode`/lookback/periodic update branch remains in current code/data docs. Fixed sidecar coverage normalization across `YYYYMMDD`, `YYYYMMDDHHMMSS`, and `YYYY-MM-DD HH:MM:SS`, and made current `cn_schedule` month-loop partitions refresh by coverage without re-pulling closed months. Added tests for datetime sidecar bounds and current-month-only macro refresh. Verification passed: compileall, `update --help`, targeted TuShare update guard tests with 8 tests OK, full unit discovery with 92 tests OK, and `git diff --check`; no live TuShare API call or large download was run.
2026-05-29 TuShare single-entry 20260529 update test: ran `scripts/tushare/download.py update --start-date 20260529 --end-date 20260529` with default intraday/share_float enabled. The new single entrypoint completed with status ok. It filled missing `daily` (5505 rows), `daily_basic` (5506), `moneyflow` (5190), refreshed `cn_schedule` current month, wrote full-market by-date minutes for 20260529 (1,326,705 rows, 5505 expected codes, 0 missing), and rebuilt `share_float_complete` at 12,736,101 rows without shrink. `margin` and `margin_detail` still returned 0 rows on both the update and a targeted retry, so no active zero-row files were written; the 20260529 event-flow probe remains error only for those two missing source-not-ready partitions. Intraday probe audit is ok with 0 errors/0 warnings. Temporary probe statuses were archived to `archive/data_quality/20260529_single_update_probe/`. Verification after the run passed: compileall, targeted TuShare tests with 9 tests OK, full unit discovery with 92 tests OK, and `git diff --check`.
2026-05-29 TuShare cron maintenance: checked official TuShare update-time docs for the retained interfaces, added `configs/tushare_update_schedule.json` with per-interface update timing and cron policy, added `scripts/tushare/cron_update.py` for locked/logged scheduled updates, added `ops/cron/install_tushare_cron.py` for managed-block crontab refresh, and installed the block while preserving the existing ChouQuant cron. Schedule is Beijing time 23:35 same-day full update plus 09:45 next-morning backfill. Verification passed: JSON validation, compileall, cron dry-runs for both jobs, full unit discovery with 92 tests OK, `git diff --check`, and crontab inspection.
2026-05-29 TuShare cron SubAgent audit follow-up: opened and closed GPT-5.5 xhigh SubAgent `Banach` for editable cron/config audit. It found a real medium issue (per-job locks could allow evening/backfill jobs to overlap) and a low issue (cron redirection depended on an existing ignored `logs/` directory). Fixed runner locking to use one global `logs/tushare_cron_global_update.lock`, changed cron lines to `mkdir -p logs` before redirecting, refreshed the installed crontab via `ops/cron/install_tushare_cron.py`, and kept the SubAgent's corrected `news` doc URL.
2026-05-29 TuShare cron runtime path cleanup: kept `ops/` for tracked operations assets, but moved cron lock/state semantics out of `logs/`. Added ignored `/.runtime/`; `scripts/tushare/cron_update.py` now writes lock to `.runtime/tushare/locks/tushare_update.lock` and state to `.runtime/tushare/cron_state.json`, while `logs/` remains only for dispatch and per-run logs. Updated data documentation accordingly.
2026-05-29 TuShare pre-open backfill adjustment: changed `cn_next_morning_backfill` from a full previous-day update to a 09:10 Beijing-time light event-flow backfill for only `margin` and `margin_detail`, matching TuShare's 09:00 update note and leaving time before 09:25 for quick audit, feature freeze, and Agent decisions. `cn_evening_full` remains the full same-day update at 23:35.
2026-05-29 TuShare pre-open margin retry split: replaced the single 09:10 margin backfill job with two light pre-open jobs, `cn_preopen_margin_backfill_0905` at 09:05 and `cn_preopen_margin_retry_0915` at 09:15. Both target previous-day `margin`/`margin_detail` only; separate job names ensure 09:15 still retries if 09:05 returned successfully while the source was not ready, while existing completed partitions still skip quickly.
2026-05-30 data documentation order and board-trading scope: reordered `docs/data_documentation.md` so initial download/compaction precedes daily update/cron operations, and documented the retained 打板 research boundary. Current data already includes `stk_limit`, `limit_list_d`, by-date 1-minute bars, liquidity/flow, suspension and name/ST history; these support board labels and minute replay, while true intraday execution must avoid `limit_list_d` lookahead and may later need QMT/Level-2 order-book data.
2026-05-30 TuShare board-trading source scan: checked TuShare official docs/MCP for 打板专题接口 and updated `docs/data_documentation.md` with candidate supplements. Recommended small-window validation before default download for `kpl_list`, `limit_step`, `limit_cpt_list`, `limit_list_ths`, `top_list/top_inst`, `hm_list/hm_detail`, `ths_hot/dc_hot`, and optional `stk_auction`; topic/sector sources should be added selectively after choosing a primary taxonomy.
2026-05-30 board_trading data domain implementation: added a dedicated TuShare `board_trading` tier/status for `kpl_list`, `limit_step`, `limit_cpt_list`, `top_list/top_inst`, `hm_list/hm_detail`, `ths_hot`, and `dc_hot`; wired it into daily `update`, cron interface metadata, audit, docs, and tests. Real window downloads for 20260529 and 20200102 succeeded; `results/data_quality/board_trading_status.json` is ok for 20260529. Full 2020-20260529 backfill is estimated at 22,627 API tasks and was not launched in this turn.
2026-05-30 THS limit-list and auction alignment follow-up: added `limit_list_ths` to the `board_trading` domain with official 20231101 start, five default THS pools, download/audit/update/cron metadata, docs, and unit coverage. Backfilled `limit_list_ths` for 20231101-20260529: 3,115 parquet/meta partitions, 114,428 rows,专项审计 ok with 0 errors/warnings and archived under `archive/data_quality/20260530_limit_list_ths/`. Added Environment PIT-layer open-auction correction for historical 09:30 minute bars: `00*.SZ` factor 0.76, `30*.SZ` factor 0.58, others 1.0. `auction-alignment` audit against `stk_auction` and daily units was ok; it confirmed the issue is concentrated in Shenzhen 09:30 auction proxy bars, while full-day minute-vs-daily units align at 100x vol and 1000x amount. Full unit discovery passed with 95 tests and `git diff --check` passed.
2026-05-31 board_trading 2020+ completion follow-up: recorded current data findings in living docs, including the `limit_list_ths` 20231101 source boundary, top-list name alias/exact-duplicate behavior, and Shenzhen 09:30 auction correction evidence. Launched `download --tier board_trading --start-date 20200101 --end-date 20260529`; it completed and skipped all expected existing partitions (`kpl_list` 7750, `limit_step` 1550, `limit_cpt_list` 1550, `limit_list_ths` 3115, `top_list/top_inst` 1550 each, `hm_detail` 926, `ths_hot` 4650, `dc_hot` 3100), confirming the 2020+ retained boundary is present. Full board-trading audit after key refinement is warning with 0 errors and 1 source-duplicate warning from `top_list` exact duplicate rows; all dataset partition counts match expected counts with 0 missing files, 0 missing sidecars, 0 orphan sidecars, and 0 pagination cap warnings. Full unit discovery passed with 95 tests and `git diff --check` passed.
2026-05-31 LLM conversation logging and repo cleanup: cleaned all `__pycache__` directories and stale empty source/script/test/ops directories. Added default DeepSeek conversation JSONL logging under ignored `data/llm_conversations/<provider>/<model>/<YYYYMMDD>.jsonl`; every API attempt records prompts/messages, raw responses, usage, hashes, and error metadata while excluding Authorization/API keys and redacting `sk-...` strings. Documented that TuShare can remain under `scripts/tushare/` as the current CLI/implementation boundary, but further growth should move stable implementation into `src/hl_trader/data_sources/tushare/` with thin script entrypoints. Verification passed: targeted DeepSeek tests 12 OK, compileall, full unit discovery 97 OK, `git diff --check`, no remaining `__pycache__` or empty stale dirs.
2026-05-31 TuShare data_sources package refactor: moved the heavy TuShare implementation from `scripts/tushare/` into `src/hl_trader/data_sources/tushare/` and kept `scripts/tushare/{download,audit,cron_update,common}.py` as thin compatibility/CLI wrappers. Updated data and pipeline docs so new TuShare implementation belongs under `src/hl_trader/data_sources/tushare/`, while command paths remain `scripts/tushare/*.py` for cron/manual use. Adjusted TuShare unit tests to patch/import the implementation package. Verification passed: direct download/audit/cron wrapper help/dry-run, targeted TuShare tests 9 OK, compileall, full unit discovery 97 OK, `git diff --check`, and post-check cleanup removed all `__pycache__`.
2026-05-31 living-doc wording cleanup: revised current data/pipeline documentation to describe only the accepted TuShare implementation and command-entry structure, removing migration-style wording such as “后续不要/旧的/仍保留/兼容 wrapper”. Keyword scan over the five living docs no longer finds those migration phrases, and `git diff --check` passed.
2026-05-31 unit-test consolidation: merged `tests/unit` from 17 `test_*.py` files into 6 domain files: `test_agent.py`, `test_agent_shadow_pipeline.py`, `test_environment.py`, `test_pipeline.py`, `test_protocol_architecture.py`, and `test_data_sources_tushare.py`. Coverage count remains 97 tests; no test logic was intentionally removed. Verification passed: compileall, full unit discovery 97 OK, `git diff --check`, and post-test `__pycache__` cleanup.
2026-05-31 TuShare nightly audit cron: added `cn_nightly_full_audit` at 02:30 Beijing time. The cron runner now supports multi-command `audit_full` jobs and refreshes the six formal data-quality status files over the retained 20200101-to-current window after the nightly update. Minute audit stays full-window inventory plus sampled deep checks by default; manual `intraday-by-date --full-scan` remains the heavy row-level path. Installed the refreshed managed crontab. Verification passed: compileall, targeted TuShare cron test, TuShare unit file 11 OK, cron dry-run/install dry-run, `git diff --check`, and resource checks.
2026-05-31 data documentation cleanup: removed the redundant 打板专题 candidate/priority table from `docs/data_documentation.md`. The section now describes only the current `board_trading` download boundary, PIT visibility rules, and when optional board/auction/sector sources should be added later. `git diff --check` passed.
2026-05-31 data documentation restructure: reorganized `docs/data_documentation.md` into numbered sections: document boundary, data domains/tables, downloads/updates, audit/status logic, Raw PIT contract, and official doc links. Audit logic now has a shared audit layer plus per-status details for base, macro, intraday, event/flow, board-trading, and text evidence. Verified heading structure, removed residual candidate-table wording, and `git diff --check` passed.
2026-05-31 living-doc navigation pass: added top navigation blocks and numbered section headings to the five current living docs: data, agent, environment, pipeline, and QMT. Reviewed their top-level logic order; data now reads data contract before update operations, while agent/environment/pipeline/QMT retain their accepted boundary-to-implementation-to-operation flow. Updated整理日期 and verified heading structure plus `git diff --check`.
2026-05-31 living-doc detailed navigation pass: expanded the five living-doc navigation blocks to include subsection links. Added natural third-level headings in Agent, Environment, Pipeline, and QMT docs so the navigation points to real anchors rather than prose-only sections. Rechecked heading structure, code-fence balance, stale candidate-table wording, and `git diff --check`; all passed.
2026-05-31 full code/docs audit follow-up: opened and closed GPT-5.5 xhigh SubAgent `McClintock` for full repository code/document audit. It found no blocking issue; fixed the high/medium findings by writing LLM conversation `started` records before provider calls, redacting sensitive dict-key values in conversation logs, normalizing text evidence source timestamps to `+08:00`, and correcting stale doc/config references. Verification passed: compileall, targeted agent/TuShare tests 46 OK, full unit discovery 101 OK, `git diff --check`, and post-test `__pycache__` cleanup.
2026-06-01 Claude TuShare cron audit validation: read `.runtime/tushare/cron_state.json`, cron logs, update/audit code, installed crontab, status files, and sampled raw daily/minute partitions. Confirmed `cn_evening_full` has failed on 20260529/20260530/20260531 at `intraday_by_date` because historical daily-vs-minute coverage gaps exceed the hard-coded existing-file tolerance; confirmed pre-open margin jobs fail on non-trading previous natural days. The `.BJ`/NEEQ historical gap diagnosis is directionally correct, but the claimed 5/29 TuShare-backfill causality is not supported locally because `daily/trade_date=20200102.parquet` was written on 2026-05-19. Recommended fix should define a proper minute-coverage expected universe and non-trading-day skip, not blindly exclude all `.BJ` codes.
2026-06-01 TuShare cron ingestion fix: fixed the two cron blockers without adding new scripts. `expected_codes_source=minute` now uses existing by-date minute coverage for already-built files and falls back to `daily` only for missing new day files; daily update and nightly audit use this source-aware minute universe, while strict `daily` coverage remains available for专项排查. `download_event_flow` now skips empty SSE trading windows successfully, so weekend/holiday pre-open margin jobs no longer report errors. Nightly audit now targets the previous natural day at 02:30. Verification passed: compileall, TuShare unit tests 14 OK, full unit discovery 103 OK, cron audit dry-run shows `--expected-codes-source minute`, `update-intraday-by-date 20200102` skipped existing file, non-trading margin backfill skipped cleanly, and `/tmp` January minute audit was ok.
2026-06-01 GitHub collaboration commit prep: removed generated Python caches, added GitHub collaboration standards to `AGENTS.md` and `CLAUDE.md`, and prepared a focused multi-commit split on a review branch. Verification before commit passed: `git diff --check`, compileall, and full unit discovery 103 OK; `check.ipynb` remains untracked and intentionally unstaged.
2026-06-01 GitHub branch naming cleanup: consolidated redundant Git/GitHub guidance in `AGENTS.md` and `CLAUDE.md`, added standard branch prefixes (`fix/`, `feat/`, `docs/`, `refactor/`, `test/`, `ops/`, `chore/`), and corrected a spelling issue in the repository-cleanliness guardrail. Verification passed: `git diff --check`, compileall, full unit discovery 103 OK, and post-test cache scan clean.
